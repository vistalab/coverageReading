%% create a gui to see coverage as well as averaged values and error bars
% 
% This script will assume that the D_{statistic} are already computed and
% stored.
%

%% a few things to modify here:

% where the D_{statistic}.mat matrices are stored
pathD = '/sni-storage/wandell/data/reading_prf';

%%
